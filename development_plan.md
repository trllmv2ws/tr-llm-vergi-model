Geliştirme Planları
Modelin çıktıları HuggingFace Space üzerinden demo olarak sunulacak (isteğe bağlı erişimle).

RAG (retrieval-augmented generation) destekli sistem ile mevzuat veritabanı bağlantısı planlanıyor.

Yeni veri setleri (GVK, KVK, KDV gibi) ile fine-tune devam edecek.

Model çıktılarının analizi için kullanıcı etkileşimli değerlendirme arayüzü tasarlanacak.
